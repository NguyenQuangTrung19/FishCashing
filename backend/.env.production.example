# Database (Production)
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=fishcash_prod
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_HERE
DB_DATABASE=fishcash_pos

# JWT (MUST change to a strong random secret)
JWT_SECRET=CHANGE_THIS_TO_A_RANDOM_64_CHAR_SECRET
JWT_EXPIRES_IN=7d

# App
PORT=3000
NODE_ENV=production

# CORS — allowed origins (comma-separated, or * for all)
# For native apps, * is fine since requests come from the device, not a browser origin
CORS_ORIGIN=*
